# {{slug}}/

A governed folder in this BaaB workspace. Notes here follow the four laws — read the
root `CLAUDE.md` for the full set.

Write notes directly here with frontmatter (`type`, `id`, `status`, dates). If this
folder hosts a spawnable kind, add new members with `baab new <kind> <slug>` rather
than by hand, and `_registry.md` will be generated for you by `baab index`.
