# clients/

Client accounts, one folder each. Spawn with `baab new client <slug>`.

Each client folder holds contacts, the services you provide them, and intake notes.
Keep client credentials as `op://vault/item/field` references, never as values.
`_registry.md` is generated by `baab index`.
