# infrastructure/

The managed tools and accounts this business runs on — one folder per app or
service. Spawn with `baab new app <slug>`.

Each app folder records what the tool is, how this business uses it, and where its
configuration and credentials live (as `op://vault/item/field` references, never
values). `_registry.md` is generated by `baab index`.
